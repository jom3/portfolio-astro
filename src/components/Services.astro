---
import {services} from '../data/services'
import ServiceCard from './services/ServiceCard.astro'
---

<section class="min-h-screen mt-20 py-20 md:py-0 w-full flex flex-col justify-center items-start p-5 md:px-20 gap-10" id="services">
  <div class="flex flex-col gap-10">
    <h1 class="text-4xl">My <span class="text-danger uppercase">services</span></h1>
    <p class="w-full md:w-[500px]">"Development and maintenance of complete web applications, optimized for performance and user experience."</p>
  </div>
  <div class="w-full flex flex-row gap-5 flex-wrap justify-evenly">
    {
      services.map((service)=>{
        return (
          <ServiceCard service={service}/>
        )
      })
    }
  </div>
</section>